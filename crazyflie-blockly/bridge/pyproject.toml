[project]
name = "drone-lab-bridge"
version = "0.1.0"
description = "WebSocket bridge between the Drone Lab UI and a Crazyflie."
requires-python = ">=3.11"
dependencies = [
    # cflib from git for the latest API; PyPI release lags.
    "cflib @ git+https://github.com/bitcraze/crazyflie-lib-python.git",
    "websockets>=12.0",
]

[tool.uv]
package = false
